# ============================================================================
# Muni Depot Configuration Example
# Copy to .env and customize, or run: ./scripts/provision.sh
# ============================================================================

# Depot Identity
DEPOT_NAME=example

# ----------------------------------------------------------------------------
# Console (Web UI)
# ----------------------------------------------------------------------------
# Password for web UI access (username: admin)
# Leave empty to disable password auth (use Tailscale instead)
CONSOLE_PASSWORD=
CONSOLE_USERNAME=admin

# ----------------------------------------------------------------------------
# InfluxDB (Metrics Database)
# ----------------------------------------------------------------------------
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=changeme
INFLUXDB_ORG=muni
INFLUXDB_BUCKET=muni
INFLUXDB_ADMIN_TOKEN=changeme

# ----------------------------------------------------------------------------
# Grafana (Dashboards)
# ----------------------------------------------------------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme

# ----------------------------------------------------------------------------
# PostgreSQL (Dispatch Database)
# ----------------------------------------------------------------------------
POSTGRES_USER=muni
POSTGRES_PASSWORD=changeme
POSTGRES_DB=depot

# ----------------------------------------------------------------------------
# Storage
# ----------------------------------------------------------------------------
# Where to store session recordings
SESSIONS_PATH=/data/muni-sessions
# How long to keep sessions (days)
RETENTION_DAYS=30

# ----------------------------------------------------------------------------
# RTK Base Station (Optional, used with --profile rtk)
# ----------------------------------------------------------------------------
# Password for NTRIP caster
NTRIP_PASSWORD=changeme
# Serial port for GPS module
GPS_SERIAL_PORT=/dev/ttyACM0
GPS_BAUD_RATE=115200

# ----------------------------------------------------------------------------
# Voice Command API (Optional)
# ----------------------------------------------------------------------------
# API key for external STT integration. Leave empty to rely on network isolation.
VOICE_API_KEY=
